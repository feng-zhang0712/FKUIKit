import Foundation

public extension Locale {
  /// POSIX locale (`en_US_POSIX`), commonly used for stable machine-readable date/number formatting.
  static var fk_posix: Locale {
    Locale(identifier: "en_US_POSIX")
  }
}
